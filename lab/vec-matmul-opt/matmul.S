.text
.align 4
.global matmul
.type matmul,@function
matmul:
	# a0 holds the dimension of the matrix n
	# a1 holds the base address of matrix 0
	# a2 hold the base address of matrix 1
	# a3 hold the base address for matrix 2
	ret
